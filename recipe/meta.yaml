{% set name = "solarmach" %}
{% set version = "0.2.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/solarmach-{{ version }}.tar.gz
  sha256: 59b87e5cd029f3f9af811c542e68f70201727a026f23189b6b00f73016407a61

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools-scm
  run:
    - astropy
    - astroquery
    - jinja2
    - matplotlib-base
    - numpy
    - pandas
    - python >=3.6
    - scipy
    - sunpy

test:
  imports:
    - solarmach
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/jgieseler/solarmach
  summary: Multi-spacecraft longitudinal configuration plotter
  license: BSD-3-Clause
  license_file: LICENSE.rst

extra:
  recipe-maintainers:
    - jgieseler
